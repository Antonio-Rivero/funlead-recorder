# deps
node_modules/
.pnp.*
# build
dist/
.next/
*.tsbuildinfo
next-env.d.ts
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/
*.app
*.dmg
# bundled ffmpeg (downloaded at build time, never committed)
apps/desktop/src-tauri/bin/ffmpeg*
# bundled whisper.cpp binary + GGML models (fetched at build time, never committed)
apps/desktop/src-tauri/bin/whisper-cli*
apps/desktop/src-tauri/bin/models/*.bin
# env / secrets
.env
.env.*
!.env.example
# recordings / media artifacts
*.mp4
*.webm
*.mov
*.wav
*.gif
# os / editor / agents
.DS_Store
**/.claude/
# local private scan patterns (real client/identity/infra strings) — never commit
# (the tracked .private-secret-patterns.example documents the format)
.private-secret-patterns
